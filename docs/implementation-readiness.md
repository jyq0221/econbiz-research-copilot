# 实施前本地检查

> 历史检查记录。现状见 [阶段 A 验证记录](agent-entry-validation.md) 和 [阶段 B 验收](stage-b-validation.md)；以下状态保留为实施前证据。

检查日期：2026-09-15。用途：为本维护仓库开始实施 Agent 入口与 Skills 提供本地基线。

本次范围：检查现有文件、运行原有测试、整理实施计划。未开始新增产品代码或创建实际研究项目。以下 Git 信息来自本地，不代表刚刚核对过远程服务器。

## 1. 结论

现有基础工具可以继续使用。开始实施时先保存一个完整的本地 Git 基线，再按“项目文件与保存 → 研究记录与方案依赖 → 入口与 Skills → 实际宿主验收”的顺序开发。

当前目录用于维护 GitHub 产品。用户研究目录的生成与管理是待开发的产品功能，开发测试在临时目录进行。

## 2. 文件及 Git 状态

| 项目 | 实际检查结果 | 实施时的处理 |
| --- | --- | --- |
| 当前分支 | `codex/framework-foundation` | 保留现有工作，建立完整基线后再开始功能变更 |
| 本地 HEAD | `f1e9bc9`，`initial commit` | 当前历史仅跟踪初始 README，不能恢复现有整套源码 |
| 已跟踪的工作文件 | `README.md`，内容有未提交修改 | 随基线审阅并提交 |
| 未跟踪文件 | `.gitignore`、`pyproject.toml`、`econbiz/`、`docs/`、`examples/`、`tests/` | 逐项审阅后纳入基线，不使用包含未知文件的全目录暂存 |
| 产品入口和 Skills | 尚无根 `AGENTS.md`、`CLAUDE.md` 及五个项目 Skills | 按实施计划创建；当前不能宣称 clone 后自然语言研究入口已交付 |
| 教学预览遗留 | `research-projects/student-preview-7kitf1xi/study/`，状态注明合成数据演示，来源为 `examples/panel.csv` | 已被忽略，本轮保留；后续测试使用临时目录，无需迁移成真实研究 |
| 本机杂项 | `.DS_Store` 被忽略 | 保持不提交 |

新写入的检查记录和实施计划也是维护文档，后续应纳入 Git。研究材料排除规则已经存在于 `.gitignore`，无需为了开发本功能先创建个人研究目录。

## 3. 可复用的实现与实际缺口

| 现有文件 | 已有能力 | 新设计需要补齐的部分 |
| --- | --- | --- |
| `econbiz/state.py` | JSON 状态、产物版本、依赖、确认、过期传播、单文件原子保存、中断标记 | 文件旧内容保存、项目检查点、相对路径解析、任务和科研记录契约；继续保留执行与核对的区别 |
| `econbiz/audit.py` | UTF-8 CSV 检查、键、缺失标记、数值合法性、文件校验 | 对接新项目目录及可移植来源引用；Excel 适配单独验收 |
| `econbiz/plans.py` | 字段与方法契约、单个数据盘点依赖、具体版本确认 | 文献/讨论依赖、Agent 直接提出方案、所有方案入口共用结果后探索检查 |
| `econbiz/candidates.py` | 固定规则教学草案、样本事实、已有结果历史识别 | 保留教学用途；路径解析和探索检查复用公共实现 |
| `econbiz/cli.py` | 现有内部命令、报告落盘 | 保持兼容；Agent 主入口调用同一 Python 能力，第一版不扩张独立 CLI 产品 |
| `README.md`、开发说明 | 如实描述当前原型 | 实施期间增加新能力状态，正式验收后更新自然语言使用步骤 |

重点处理三个实际衔接点：

1. `Project.revise()` 当前会保存旧状态内容，但未保存任意文件的旧字节；不能把它当作完整文件版本管理。
2. CSV 盘点记录和候选草案读取当前使用 `input_path` 的绝对路径；搬动用户项目时需要可移植引用及旧数据兼容。
3. `register_plan()` 当前只绑定一个 inventory；换成 LLM 提案后，文献、研究讨论和结果后探索不能脱离公共依赖与确认规则。

## 4. 本次验证

| 检查 | 结果与范围 |
| --- | --- |
| Python | 本机 `Python 3.9.6`，符合当前 `>=3.9` 声明 |
| 全部既有单元/流程测试 | `python3 -m unittest discover -s tests -v`：56 项通过，0 失败 |
| 已跟踪修改的空白检查 | `git diff --check` 通过；该命令本身不覆盖未跟踪文件 |
| 待发布目录的定向文本检查 | 对源码、文档、示例和测试搜索本机绝对路径及常见密钥赋值模式，未命中；仅是限定模式检查 |
| 教学预览性质 | 状态注明“合成数据演示”，盘点指向随库教学 CSV |

测试通过证明现有基础行为，不代表新的 Skills、科研记忆、文件恢复、跨宿主使用或统计估计已经验证。

## 5. 实施交接

- 设计依据：[入口与 Skills 设计方案 v0.3](superpowers/specs/2026-09-15-agent-entry-skills-design.md)。
- 下一步执行：[阶段 A 实施计划](superpowers/plans/2026-09-15-agent-entry-skills-implementation.md)，从任务 0 开始。
- 阶段 A 面向研究准备、材料与进度管理；正式计算与解释闭环沿设计的阶段 B 单独交付。
- 本轮不改变现有分支、暂存区或提交历史。下一轮实施先按计划保存基线；推送或对外发布另按明确要求执行。
