# 阶段 B 试用版发布检查

日期：2026-09-16；版本：0.2.0a1；实现提交：e7aa6ef。

## 发布前结论

可按当前功能边界发布试用版。用户已授权通过检查后推送。main 继续使用开发分支的 git archive 导出树，开发材料留在 codex/framework-foundation；不合并两分支、不重写历史。

| 检查 | 实际结果 |
| --- | --- |
| 全套测试 | 151 项通过，14.406 秒；启用 -W error |
| 阶段 A 无可选依赖 | -S 模式套件通过，37 项依赖统计/Excel 的测试跳过 |
| Skills 同步 | 五个维护源与普通文件副本一致 |
| 使用包检查 | 四项 ZIP/clone 分发测试通过；本地链接和两套入口完整 |
| 干净安装 | 临时 Python 3.9.6 虚拟环境，从导出树构建并安装 wheel 及 analysis 固定依赖；pip check 通过 |
| 安装后的真实执行 | 从已安装的 site-packages 导入，72 个合成观测的双向固定效应回归 completed/passed |
| 冻结包和恢复 | 冻结包独立重跑的全部参数一致；输入、盘点、方案、运行及报告重新打开后均有效 |
| 报告 | HTML、Markdown、CSV、JSON 四种输出均保存 |

合成验收的 x 系数为 1.134150111014766，c 系数为 0.4553360113039632；与实现验收一致。测试使用临时合成项目，不包含个人研究数据。

发布前仅修正 README 试用版名称，以及开发说明中的运行状态和手册数量；统计实现未改动。完整统计、故障注入及 Skills 实测依据见 [实现验收](stage-b-validation.md)。

## 发布范围

Python 描述统计、企业/年度固定效应、传统/HC1/单维聚类标准误、独立数值复核、运行包与结果报告。Stata、DID、IV、权重和多维聚类仍未接入。环境实测为 macOS arm64、Python 3.9.6；跨宿主和真实初学者试用仍需另行验收。

## 远端验收

发布前检查已完成；推送后在此追加远端提交、实际默认 clone 和 ZIP 的文件及执行验证结果。
