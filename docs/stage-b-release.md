# 阶段 B 试用版发布检查

日期：2026-09-16；版本：0.2.0a1；实现提交：e7aa6ef。

本页保留 0.2.0a1 的历史发布证据；最新 0.2.0a3 的 main/ZIP 检查见 [自动分析与双引擎发布记录](agent-analysis-release.md)。

## 发布前结论

可按当前功能边界发布试用版。用户已授权通过检查后推送。main 继续使用开发分支的 git archive 导出树，开发材料留在 codex/framework-foundation；不合并两分支、不重写历史。

| 检查 | 实际结果 |
| --- | --- |
| 全套测试 | 151 项通过，14.406 秒；启用 -W error |
| 阶段 A 无可选依赖 | -S 模式套件通过，37 项依赖统计/Excel 的测试跳过 |
| Skills 同步 | 五个维护源与普通文件副本一致 |
| 使用包检查 | 四项 ZIP/clone 分发测试通过；本地链接和两套入口完整 |
| 干净安装 | 临时 Python 3.9.6 虚拟环境，从导出树构建并安装 wheel 及 analysis 固定依赖；pip check 通过 |
| 安装后的真实执行 | 从已安装的 site-packages 导入，72 个合成观测的双向固定效应回归 completed/passed |
| 冻结包和恢复 | 冻结包独立重跑的全部参数一致；输入、盘点、方案、运行及报告重新打开后均有效 |
| 报告 | HTML、Markdown、CSV、JSON 四种输出均保存 |

合成验收的 x 系数为 1.134150111014766，c 系数为 0.4553360113039632；与实现验收一致。测试使用临时合成项目，不包含个人研究数据。

发布前仅修正 README 试用版名称，以及开发说明中的运行状态和手册数量；统计实现未改动。完整统计、故障注入及 Skills 实测依据见 [实现验收](stage-b-validation.md)。

## 发布范围

Python 描述统计、企业/年度固定效应、传统/HC1/单维聚类标准误、独立数值复核、运行包与结果报告。Stata、DID、IV、权重和多维聚类仍未接入。环境实测为 macOS arm64、Python 3.9.6；跨宿主和真实初学者试用仍需另行验收。

## 远端验收

已正常、原子推送 main 和开发分支，远端默认分支仍为 main：

- main：`a90adc2f3931823f1a86df6aa1f6ce983e1c7e71`。
- 导出源开发提交：`9407bdcfa67a5d1011c7e577ea44b764a3a6f051`；本记录的后续补充仅保存在开发分支。
- [当前使用包](https://github.com/jyq0221/econbiz-research-copilot/archive/refs/heads/main.zip) 的实下载 ZIP 注释标识与 main 提交相同；SHA-256 为 `bad403e5867d7ff84c02093ad88b6f95201ab8da6a6bc364e23b8ace56de264a`。

从公开仓库地址重新默认 clone（未指定分支），并从 README 链接实际下载 ZIP：56 个文件的路径与字节全部一致，也与开发导出树一致。两套 Skills、四份使用手册和反馈模板完整，本地 Markdown 链接全部可解析；未包含 CSV、XLSX、数据 ZIP、测试或内部开发材料。

分别从 clone 和 ZIP 解压目录启动独立 Python 进程，并断言实际导入来自各自交付目录：72 行回归 completed/passed，全部参数与干净安装验收一致；冻结包重跑参数一致，四种报告保存并重新打开有效。最终两次均在 -W error 下通过且无警告。首轮验收启动脚本的文件关闭警告已定位为临时脚本读取方式，改为自动关闭文件后复测，未修改发布的统计代码。

独立测试数据附件也从公开链接重新下载，SHA-256 仍为 `a479b5d12d48e8a8b40a2e558f6bed8a88563b93ddf27f2a3f7a05dd88733b8d`，包内逐文件散列通过。用已发布的结构检查器确认完整版本 240 行、30 家企业、8 年、passed；问题版 243 行、2 组重复键、2 条缺失键、1 条非法数值、5 个缺失单元格、failed；有效零保留。附件内容未改动。
