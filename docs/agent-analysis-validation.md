# Agent 分析与项目脚本验收（0.2.0a3）

日期：2026-09-16。开发分支：`codex/framework-foundation`。本记录对应项目脚本工作流的第一批实现；不表示 main/ZIP 已发布，也不表示所有方法均已具备通用数值核验。

## 自动检查

- 启用本机真实 Stata 19 的全套测试：**239 项通过，0 跳过**，114.362 秒。
- `python3 -S` 的标准库环境：239 项中 **149 项通过、90 项按依赖/原生环境范围跳过**，9.319 秒；不自动探测或启动 Stata。
- Skills 源与普通副本同步检查通过；`git diff --check` 通过。
- 27 项预处理专项测试含真实 Stata。默认测试不启动 Stata，只有设置 `ECONBIZ_TEST_STATA` 才启用原生案例；通用原生项目脚本测试使用 `ECONBIZ_STATA_EXECUTABLE` 显式启用。

复现全套原生测试时，两项环境变量均设为实际已安装的 Stata 可执行入口，再运行 `python3 -m unittest discover -s tests`。不得根据应用名称猜测授权版本；身份由现有实际探针记录。

## 真实工作流验收

### 自动化集成场景

合成企业年度数据 → 按年缩尾/明确 log1p → Python 项目处理脚本实际运行 → NumPy/pandas 独立处理核对 → 处理后数据与来源运行绑定 → 盘点 → 固定效应正式执行与独立统计复核 → 正式报告。

另执行未在固定估计注册表内的 OLS 项目脚本，实际系数与独立 NumPy 最小二乘参考一致；项目脚本仍保持 `completed/pending`，没有把测试对照升级为通用方法认证。正式固定效应仍为 `completed/passed`。否定处理运行证据后，下游来源、盘点、模型与报告随依赖关系失效。

### 自然语言宿主场景

独立执行任务只读取入口、Skills 与公开手册，未读取测试或实现源码。测试请求已授权按年 1%/99% 缩尾 leverage、对正值 assets 取 ln，要求自行判断贷款获批的关联模型，自动编程和修复技术错误，使用 Python。

- 实际读取 480 条观测、80 家企业、2018—2023 年及字段说明；没有缺失或重复键，资产全部为正。
- 推荐并执行 Logit，含年度/行业控制、企业聚类标准误；没有为迁就内置估计器改成固定效应线性模型，也没有探测 Stata。
- 缩尾影响 12 条杠杆记录；480 条资产构造 ln。原列、前导零及全部观测保留。
- 独立数据处理核对 10 项全部通过。主任务以最终代码重新核对同一冻结输入、规则和输出，仍全部通过。
- 显式注入一个合成语法错误，保存失败日志；仅修复该错误后同方案、输入和引擎重跑成功。代码从 v1 升至 v2，原失败当前为 stale，冻结内容保留原始 failed/failed；新运行准确绑定 retry_of。
- Logit 实际收敛（4 次迭代），设计矩阵满秩；交付完整系数、协方差、样本、诊断、解释及 Python 源码。其状态保持 `completed/pending` 和 `statistical_verification=not_performed`，不声称独立统计认证。
- 会话 v2 保存后重新打开，核对全部运行文件散列、处理数据对原运行的依赖及会话输出；无额外用户确认。
- 另做 DID/IV 只读情境：保留因果问题，指出处理时点与比较组尚未核实；继续说明可做的数据检查，不编造 DID。拒绝按不显著结果或最高相关性自动选 IV，不新建研究或改变实际研究状态。

本机合成证据目录：`/private/tmp/econbiz-agent-workflow-hpp8cw32/`。主要证据为 `host-acceptance.json`、`final-preparation-check.json`、`交付说明.md` 及 `study/research/runs/`。这是临时验收材料，不是用户研究或随发行包交付的数据。

宿主测试发现公开手册缺少完整方案/会话字段示例，修订后同一任务仅依靠公开手册完成了执行与保存。已补齐示例，避免让使用者或 Agent 逐项试探接口。

## 审查发现与修复

规格与质量审查使用额外错误注入及真实原生探针，保留了发现失败的测试：

- 处理来源必须依赖产生它的实际运行，不能只依赖源码；旧来源恢复也遵守这一关系。
- 超时前已打印的输出流写入文件；一个非法输出不会妨碍其他可读证据登记。
- 宿主中断保留可能的结果暴露与失败状态，允许新运行技术重试。
- 显式项目脚本路径才能调用通用运行器；自报 passed 文件不能升级统计状态。
- Python 内部入口/工具路径不泄漏到分析脚本的 argparse 参数。
- 原生数值用可往返的序列化，审计 JSON 合法；UTF-8 错误、无法保真的整数阈值和不支持的派生数值文本操作明确拒绝。
- 主计算与独立参考分别修复大/小量级标准化、常数组均值、固定小数解析及大基数小变化问题；能接受正确值并拒绝伪造零值和错误偏移。

最终预处理复审无待修复发现；项目脚本质量审查所发现的 argparse 问题已用失败→通过测试修复，全套回归通过。

## 明确范围

研究判断由宿主 Agent 结合材料完成；本工具不按显著性自动搜索模型。DID、IV、权重和多维聚类可进入具体项目代码工作流，其通用独立统计核验器属于后续逐项扩展。

任意项目代码的复现覆盖登记的输入、源码和记录环境。原生默认仅搜索官方 BASE 与本次工作目录；第三方代码须随包登记。未声明的外部 adopath、文件或动态网络依赖不在复现保证内。运行器沿用宿主权限，不是任意代码沙箱。
