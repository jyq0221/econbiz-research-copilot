# 经管研究助手项目入口

先判断用户的实际任务。开发、测试、解释或维护本仓库时按软件任务处理，测试使用临时目录，不自动创建个人研究。

研究请求读取 [econbiz-research](.agents/skills/econbiz-research/SKILL.md)，再按当前问题读取所需专项 Skill。普通概念问答可直接讨论，无需建项或任务卡。用户明确点名专项 Skill 时尊重选择，只补充必要上下文。

实际研究从工具仓库根加载入口，在用户指定项目或 `research-projects/<标识>/` 保存。开始前定位项目，多个项目且指代不明时澄清；损坏的状态不能当成没有项目。按材料阅读、事实核对、研究选择、实际保存和恢复推进。

已给出的授权持续有效；先完成可审阅的方案再就尚未授权的实质选择请求决定。自动选择 Skill 无需用户审批，也不自动启动多个 Agent。

状态操作使用仓库公共 Python 接口，按实际返回值说明是否保存。每项研究共用 literature/data/research 目录、记录和版本；重要更正及时保存，实质工作结束记录下一步和授权。以当前有效材料核对结论，保持单个写入者。

阶段 A 提供研究准备、CSV 结构检查、记录与文件恢复。阶段 B 已接入有明确规则的描述统计、企业/年度固定效应、运行包与独立数值复核，调用前读正式分析手册。默认 Python，无 Stata 也可完整使用；显式选择 Stata 时才检查并启动原生引擎，首批仅验收 macOS arm64 / Stata 19，两条路径均由独立 Python 实现复核。不得把代码草稿、外部回归表或方案确认写成已执行、已复现的结果。

默认 `main` 分支和 ZIP 为使用版本。维护、测试与实施记录位于 `codex/framework-foundation` 开发分支；维护任务先定位该分支，不在使用版本中寻找被移出的开发文件。

在开发分支维护 Skills 时以 `.agents/skills/` 为源，用 `python3 scripts/sync_skills.py` 同步普通文件副本，发布前运行 `--check` 和项目测试；实际宿主行为另行验收。
